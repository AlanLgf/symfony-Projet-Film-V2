{# src\Film\AdminBundle\Resources\views\Genre\list.html.twig #}
{% extends 'FilmBiblioBundle::layout.html.twig' %}

{% block title %}Liste des genres{% endblock %}

{% block body %}

<h1 class="monh1">Liste des genres :</h1>
<ul>
{% for genre in genres %}
<div class ="well monwell">
	<div class ="row">
    <li>
        <a href="{{ path('admin_genre_modif', {'id': genre.id}) }}" class="nom_liste">{{ genre.nom }}</a>
        <a href="{{ path('admin_genre_supprimer', {'id': genre.id}) }}"  class = "btn btn-danger goright">Supprimer</a>
    </li>
    </div>
    </div>
{% endfor %}
</ul>
<a href="{{ path('admin_genre_ajout') }}" class = "btn btn-primary monw" style="margin:0 125px;">Ajouter</a>

{% endblock %}